# DGGT Studio 运行依赖（4DGS 渲染引擎 + Studio 后端）
# 注意：torch / gsplat 需与你的 CUDA 版本匹配，建议参考官方安装说明单独安装。

torch>=2.0.0
gsplat>=0.1.0
numpy>=1.24
scipy>=1.10
opencv-python>=4.8
plyfile>=0.8
imageio>=2.31

# Studio 后端 (FastAPI)
fastapi>=0.104
uvicorn[standard]>=0.24
python-multipart>=0.0.6
websockets>=12.0
